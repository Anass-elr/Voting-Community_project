package ma.elrhazi.votingservice.service;


import lombok.AllArgsConstructor;
import ma.elrhazi.votingservice.entities.LeagArticle;
import ma.elrhazi.votingservice.repositories.ArticleRepository;

import java.util.List;


public class ValoArticleController  {

/*
    public ValoArticleController(ArticleRepository articleRepository) {
        super(articleRepository);
    }
/*
    public List<LeagArticle> getAllLeagArticles() {

        return getArticleRepository().findAll().stream()
                .filter(ar -> (ar instanceof LeagArticle))
                .map(ar -> (LeagArticle) ar)
                .toList();
    } */

}

